{
  "name": "tetris-battle",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "dev:core": "pnpm --filter game-core dev",
    "build:all": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "generate-icons": "node scripts/generate-ability-icons.js"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "replicate": "^1.4.0",
    "typescript": "^5.3.3"
  }
}
